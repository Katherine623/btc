# 比特幣（Bitcoin）強化學習交易模型專題報告

## 一、研究動機

比特幣市場具有高波動、長時間連續交易與價格變化快速等特性，因此非常適合作為強化學習在金融交易決策上的示範場景。傳統交易策略通常依賴固定規則，例如均線交叉、RSI 超買超賣或人工設定的進出場條件；但在實際市場中，價格行為往往會隨時間改變，單一規則不一定能長期有效。

本專題希望建立一套以比特幣歷史資料為基礎的交易模型，讓代理人透過與市場資料互動，自動學習在不同狀態下應該選擇買入、賣出或持有，進而提升投資組合績效。為了讓成果更容易展示與操作，本專題同時使用 Streamlit 建立互動介面，使使用者可以下載資料、調整超參數、訓練模型並查看結果。

## 二、研究目標

本專題的目標如下：

1. 建立一個以比特幣歷史價格資料為基礎的交易環境。
2. 使用 PPO（Proximal Policy Optimization）訓練交易代理人。
3. 將技術指標與投資組合狀態整合為模型 observation。
4. 透過累積報酬率、Sharpe Ratio 與最大回撤評估策略品質。
5. 以 Streamlit 製作互動式展示系統，提升專題完整度。

## 三、研究方法

### 1. 資料來源

本專題使用 `yfinance` 下載 `BTC-USD` 歷史價格資料，也支援使用者上傳自備 CSV 檔案。資料至少需包含以下欄位：

- Datetime
- Open
- High
- Low
- Close
- Volume

程式可自動辨識常見欄位名稱，例如 `date`、`timestamp` 或小寫欄位名稱，並統一整理成內部使用格式。

### 2. 特徵工程

為了讓代理人能根據市場狀態進行決策，本專題針對價格資料建立 13 個技術指標特徵：

1. `return_1`：單期報酬率
2. `ma_5`：5 期移動平均
3. `ma_10`：10 期移動平均
4. `ma_20`：20 期移動平均
5. `volatility_10`：10 期波動率
6. `rsi_14`：14 期 RSI
7. `macd`
8. `macd_signal`
9. `macd_hist`
10. `close_over_ma5`
11. `close_over_ma10`
12. `close_over_ma20`
13. `vol_ratio`：成交量相對 20 期均量的比例

在訓練前，所有特徵會進行標準化處理，以降低尺度差異對模型學習的影響。

### 3. 環境設計

本專題建立 `BitcoinTradingEnv` 作為自訂 Gymnasium 交易環境。環境中的代理人只允許兩種持倉狀態：

- `0`：空倉
- `1`：滿倉持有比特幣

動作空間為離散三種：

- `0`：Hold，維持現有部位不動
- `1`：Buy，若目前空倉則使用全部資金買入 BTC
- `2`：Sell，若目前持有 BTC 則全部賣出

每一步 observation 由兩部分構成：

1. 市場技術指標特徵
2. 代理人自身狀態

代理人自身狀態包含：

- 當前持倉（position）
- 現金餘額相對初始資金比例
- 當前淨值相對初始資金比例

### 4. Reward 設計

reward 以投資組合淨值變化率表示：

```text
reward = (net_worth_t - net_worth_{t-1}) / net_worth_{t-1}
```

此外，在買入與賣出時會計入手續費 `trade_fee`，因此 reward 會自然反映頻繁交易帶來的成本。這種設計能讓代理人在學習過程中同時考慮獲利與交易代價。

## 四、提出方法與系統

### P（意圖）

本研究目標是建構一個可在高波動市場中穩定執行的自動化決策系統，使代理人不再依賴單純追價，而是透過結構化特徵與交易摩擦建模，在可控風險下追求長期資本成長。核心設計原則如下：

1. 嚴格因果性：特徵只能使用當下可觀測資訊。
2. 執行一致性：策略輸出與環境成交規則需一致。
3. 風險優先：報酬最大化必須受回撤控制約束。

### Q（設計）

系統採三層次解耦架構：

1. Data and Feature Layer（資料與特徵層）
	- 使用 OHLCV 與技術指標構成多維狀態向量，所有特徵以時間因果方式計算，不使用未來資訊。
	- 以固定窗口滾動計算特徵，並在切分資料後分別標準化，避免訓練集與測試集資訊滲漏。

2. Agent Layer（智能體層）
	- 使用 Stable-Baselines3 的 PPO（Actor-Critic）作為核心學習器。
	- PPO 以 clipped objective 限制策略更新幅度：

$$
L^{CLIP}(\theta)=\mathbb{E}_t\left[\min\left(r_t(\theta)\hat{A}_t,\;\mathrm{clip}(r_t(\theta),1-\epsilon,1+\epsilon)\hat{A}_t\right)\right]
$$

3. Environment Layer（事件驅動回測層）
	- 建模交易摩擦：基礎手續費、Maker/Taker 費率、動態滑價與價差成本。
	- 建模市場限制：最小下單金額、最小數量、價格與數量步進。
	- 建模風險約束：最大回撤停機、虧損停機、波動目標縮放。

### R（描繪）

本研究將交易問題形式化為馬可夫決策過程（MDP）如下：

1. 狀態空間 $S_t$
	- 市場特徵向量：由報酬、均線、波動、RSI、MACD、成交量比例等特徵組成。
	- 代理人狀態：持倉比例、現金比例、淨值比例。

2. 動作空間 $A_t$
	- 本系統採離散動作：$A_t \in \{0,1,2\}$，分別代表 Hold、Increase Position、Decrease Position。
	- 實際持倉為連續比例 $p_t \in [0,1]$，並由 `position_step` 逐步調整，確保與現貨市場可行性一致。

3. 獎勵函數 $R_t$
	- 以風險覺知形式定義：

$$
R_t = \Delta PnL_t - \lambda \cdot DD_t - \eta \cdot \mathrm{TurnoverPenalty}_t
$$

其中，$\Delta PnL_t$ 為單步淨值變化，$DD_t$ 為回撤相關懲罰，$\mathrm{TurnoverPenalty}_t$ 反映頻繁調倉造成的成本磨損。此設計避免模型只追求短期報酬而忽略風險與交易成本。

4. 執行一致性
	- 策略先輸出目標持倉，再由環境根據成本與限制決定可成交部位，最後更新資產與獎勵。
	- 透過此機制，訓練過程中的 state transition 與真實可執行交易保持一致。

### S（驗證）

為降低過擬合與單次回測偏誤，本研究採用以下驗證流程：

1. Walk-Forward Validation（滾動前向驗證）：依時間窗重複訓練與測試，檢驗跨市場週期穩健性。
2. 成本敏感度分析：在不同費率、滑價、價差設定下比較策略績效。
3. 因子消融分析：比較含與不含關鍵特徵組合之模型，檢驗特徵貢獻。

## 五、模擬與實驗

### T（對標）

本研究採多基準比較，確保結果可解釋：

1. Buy and Hold（B and H）：同區間買入持有 BTC，代表市場 Beta。
2. 傳統動能策略：MACD 與均線交叉策略，代表規則式技術分析方法。
3. Vanilla PPO：僅使用基礎 OHLCV 與 RSI 的 PPO，作為 RL 消融基準。

### U（實驗參數）

1. 資料範圍：以 BTC 歷史資料進行實驗，涵蓋多個市場狀態區間，並依時間先後切分。
2. PPO 參數：

```python
learning_rate = 3e-4
n_steps = 2048
batch_size = 64
n_epochs = 10
gamma = 0.99
gae_lambda = 0.95
clip_range = 0.2
ent_coef = 0.01
```

3. 交易摩擦：納入 maker_fee、taker_fee、slippage_bps、spread_bps，並在壓力測試中提高成本情境檢驗魯棒性。
4. 風險參數：設定 max_drawdown_limit、daily_loss_limit、volatility_target 以控制極端風險。

### V（實用價值）

1. 核心指標

$$
\mathrm{Cumulative\ Return} = \frac{V_T}{V_0} - 1
$$

$$
\mathrm{Sharpe} = \sqrt{252}\cdot\frac{\mu_r}{\sigma_r + 10^{-8}}
$$

同時回報 Sortino Ratio 與 Maximum Drawdown（MDD），全面評估報酬與下行風險。

2. 極端市況檢驗
	- 在高波動與趨勢反轉階段檢查模型是否主動降倉、降低回撤與過度交易。

3. 落地性
	- 由於環境已納入成交限制與摩擦成本，實驗結果相較理想化回測更具可轉移性。

## 六、Streamlit 展示系統

為了提升專題展示效果，本專題使用 Streamlit 建立互動式介面，主要功能如下：

1. 選擇資料來源：從 yfinance 下載或上傳 CSV
2. 設定下載期間與 K 棒週期
3. 調整初始資金與交易手續費
4. 調整訓練步數與訓練集比例
5. 顯示測試績效指標
6. 顯示 RL 與 Buy and Hold 的資產曲線比較
7. 顯示買賣訊號與動作分佈圖

此設計不僅能展示最終結果，也方便觀察不同超參數對模型表現的影響。

## 七、結果分析

本專題目前屬於教學型與展示型實作，已能完整呈現強化學習應用於交易策略的基本流程。從系統設計上可以看出：

1. 強化學習可將市場資料、部位狀態與報酬函數整合成可學習的決策問題。
2. PPO 適合處理這類高維 observation 與序列決策情境。
3. 技術指標能作為市場狀態的基礎描述，協助代理人判斷買賣時機。
4. 透過與 Buy and Hold 對照，可較清楚理解模型策略是否具備額外價值。

不過，本專題也存在幾項限制：

1. 目前屬於單資產、單市場設定，尚未納入跨市場與跨資產配置。
2. 雖已納入成本與執行限制，但尚未納入資金費率、沖擊成本校準與實盤撮合延遲。
3. Reward 設計仍需進一步做參數敏感度與穩健性檢定。
4. 訓練結果仍可能受資料區間與隨機初始化影響，需以多次重複實驗報告信賴區間。

因此，現階段成果較適合作為課堂專題、強化學習入門範例與金融交易環境建模示範。

## 八、結論

本專題成功實作一個結合比特幣交易資料、技術指標與 PPO 強化學習模型的交易系統。透過自訂交易環境，代理人能在歷史資料上學習買入、賣出與持有策略，並透過測試資料計算累積報酬率、Sharpe Ratio 與最大回撤等績效指標。

同時，透過 Streamlit 介面，本系統具備完整的互動展示能力，使使用者能更直觀理解資料下載、模型訓練、策略評估與視覺化呈現的整體流程。

## 九、未來擴充方向

未來可從以下方向進一步擴充：

1. 加入更多技術指標、鏈上資料或市場情緒特徵。
2. 支援部分倉位控制與風險管理機制。
3. 增加 walk-forward validation 與更完整的回測架構。
4. 比較 PPO、A2C、DQN 等不同強化學習方法。
5. 導入多資產配置與更接近實務的投資場景。

## 十、摘要

本研究以比特幣交易為主題，建立一套基於 PPO 的強化學習交易模型，並結合 Streamlit 製作互動式展示系統。系統使用 BTC 歷史價格資料與多種技術指標作為 observation，透過自訂交易環境讓代理人學習買入、賣出與持有決策。模型績效以累積報酬率、Sharpe Ratio、最大回撤與 Buy and Hold 基準策略作為比較依據。整體而言，本專題已成功呈現強化學習如何應用於金融交易問題，具備課堂展示、學習研究與後續擴充的價值。